describe('Payment Plan Tests', function() {

    beforeEach(() => {
        cy.visit('/{prodseasonId}/{performanceId}')
      })

    it('Single Perf - Eligible Payment Plan Check"', function() {
     // select quantity and price types and click purchase

     //Click checkout on cart page

     //on Login page, login in with valid credentials

     //On the Payment Page, check that the isEligible is True, id=tn-payment-plans-eligible

     //check the amount eligibleProducts > amount


    })
  })